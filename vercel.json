{
  "version": 2,
  "buildCommand": "cd server && rm -rf node_modules && npm install --ignore-scripts && npm run build",
  "outputDirectory": "server/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index.js"
    }
  ]
}