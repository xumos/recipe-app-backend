{
  "version": 2,
  "buildCommand": "pnpm install --ignore-scripts && pnpm --filter server build",
  "outputDirectory": "server/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index.js"
    }
  ]
}